import { Typography } from "@mui/material";

import { Wrapper } from "./styles";

export const Filters = () => {
  return (
    <Wrapper>
      <Typography variant="body1">Living Room</Typography>
    </Wrapper>
  );
};
