import { Box } from "@mui/material";
import styled from "styled-components";

export const Wrap = styled(Box)`

     img {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
`